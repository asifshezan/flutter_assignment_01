import 'package:flutter/material.dart';
import 'package:assignment_02/Home.dart';

class AsifApp extends StatelessWidget {
  const AsifApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Greeting App",
      home: Home(),
    );
  }
}
